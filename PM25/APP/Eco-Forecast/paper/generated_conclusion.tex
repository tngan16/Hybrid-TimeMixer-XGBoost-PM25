The hybrid won 0/25 matched comparisons against the strongest non-hybrid comparator, corresponding to a 23.33\% average MAE gap. LSTM and DLinear were practically tied, with DLinear leading RMSE and sMAPE. Common-history sensitivity changed hybrid MAE by only \( +0.012 \), so claims are restricted to the matched protocol: gating improves compact TimeMixer and controls over-correction, but this implementation is not a superior forecaster.
