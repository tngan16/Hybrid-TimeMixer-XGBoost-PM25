# Model artifacts

This project trains models through the reproducible scripts in `Models and Data Cleaning/scripts/` and stores evaluation outputs in `results/`.

Large binary model weights are not required for the submitted research dashboard. The app reads completed experiment artifacts instead of serving live forecasts.
